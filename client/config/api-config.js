// If we want to simulate IRL conditions, this won't be included in any documentation so the students will need to ask around (e.g. ask the task moderator) to get this.
// Might want to ask Jonny about this one but we could put something like 'ask your invigilator if you're unsure about this question'.
export const apiBasePath = process.env.BASEPATH;
